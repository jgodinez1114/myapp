const express = require('express');
const app = express();
const port = 3000;



// handle get requests when a request is made to the hompage
app.get('/', (req, res) => {
    res.send('Hey yalghl');
})

// listen on given port
app.listen(port, () => {
    console.log(`ex app listening at http://localhost:${port}`);
})

